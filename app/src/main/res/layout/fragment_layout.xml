<com.parallaxstudios.careelder.DashboardLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:layout_weight="1"
    android:background="?attr/selectableItemBackground" >
	<!--  News Feed Button -->
    <Button
        android:id="@+id/btn_news_feed"
        style="@style/DashboardButton"
        android:drawableTop="@drawable/btn_cekkondisi"
        android:onClick="onCekKondisiClick"
        android:textColor="#27ae60"
        android:text="Cek Kondisi" />
    
	<!--  Friends Button -->
    <Button
        android:id="@+id/btn_friends"
        style="@style/DashboardButton"
        android:drawableTop="@drawable/btn_sayhello"
        android:onClick="onSayHelloClick"
        android:textColor="#2c3e50"
        android:text="Say Hello" />
    
	<!--  Messages Button -->
    <Button
        android:id="@+id/btn_messages"
        style="@style/DashboardButton"
        android:drawableTop="@drawable/btn_panic"
        android:onClick="onPanicClick"
        android:textColor="#c0392b"
        android:text="Panic" />
	
    <!--  Places Button -->
    <Button
        android:id="@+id/btn_places"
        style="@style/DashboardButton"
        android:drawableTop="@drawable/btn_about"
        android:onClick="onAboutClick"
        android:textColor="#2980b9"
        android:text="About" />

</com.parallaxstudios.careelder.DashboardLayout>